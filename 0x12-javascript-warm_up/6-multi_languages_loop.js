#!/usr/bin/node
const languages = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (const line in languages) {
  console.log(languages[line]);
}
// languages.forEach(line => {
//   console.log(line);
// });
